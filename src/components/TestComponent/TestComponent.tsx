import React from 'react';

import styles from './TestComponent.module.scss';

const TestComponent = () => {
	return (
		<>
			<p className={ styles.text }>React template</p>
		</>
	);
};

export default TestComponent;
